
# Отчёт о тестировании интеграции карты маршрутов

**Дата:** 2025-01-27  
**Версия:** 1.0  
**Статус:** Требуются исправления перед запуском

---

## 1. Проверка корректности настроек

### 1.1. Переменная окружения NEXT_PUBLIC_YANDEX_MAPS_API_KEY

**Статус:** ✅ Настроено правильно

**Проверка:**
- В `frontend/src/app/layout.tsx` используется правильный формат:
  ```typescript
  src={`https://api-maps.yandex.ru/2.1/?lang=ru_RU&apikey=${process.env.NEXT_PUBLIC_YANDEX_MAPS_API_KEY}`}
  ```

**Проблема:**
- ❌ Файл `.env.local` отсутствует
- ❌ Переменная окружения не задана

**Рекомендация:**
1. Создать файл `frontend/.env.local`:
   ```
   NEXT_PUBLIC_YANDEX_MAPS_API_KEY=ваш_api_ключ_здесь
   ```
2. Получить API ключ: https://developer.tech.yandex.ru/services/

### 1.2. Формат загрузки скрипта Yandex Maps

**Статус:** ✅ Правильный формат

**Проверка:**
- Скрипт загружается с атрибутом `defer`
- Используется template literal для динамической подстановки API ключа
- Правильный URL: `https://api-maps.yandex.ru/2.1/?lang=ru_RU&apikey=...`

**Рекомендация:**
- Добавить обработку случая, когда API ключ отсутствует (fallback на Leaflet или сообщение об ошибке)

### 1.3. Инициализация карты

**Статус:** ⚠️ Не может быть проверена без API ключа

**Проверка кода:**
- `YandexMapProvider` проверяет наличие `window.ymaps` перед инициализацией
- Есть обработка ошибок при отсутствии API

**Проблема:**
- Нельзя проверить без реального API ключа

---

## 2. Результаты E2E тестов

### 2.1. Статус запуска тестов

**Результат:** ❌ Тесты не запустились

**Причина:** Ошибка компиляции Next.js при запуске dev-сервера

### 2.2. Найденные проблемы компиляции

#### Проблема 1: Модуль не найден — '../route-map/ui'

**Ошибка:**
```
Module not found: Can't resolve '../route-map/ui'
```

**Местоположение:**
- `frontend/src/modules/routes/features/route-details/ui/route-details-view.tsx:9`

**Импорт:**
```typescript
import { RouteMapSection } from '../route-map/ui';
```

**Анализ:**
- Структура папок корректна: `route-details/ui/` и `route-map/ui/` находятся на одном уровне
- Файл `route-map/ui/index.ts` существует и экспортирует `RouteMapSection`
- Проблема может быть связана с:
  1. Next.js не разрешает относительные пути между features
  2. Отсутствует расширение `.ts` в импорте
  3. Проблема с кэшем Next.js

**Рекомендация:**
1. Попробовать абсолютный импорт:
   ```typescript
   import { RouteMapSection } from '@/modules/routes/features/route-map/ui';
   ```
2. Или использовать barrel export из `features/index.ts`
3. Очистить кэш Next.js: `rm -rf .next`

#### Проблема 2: Модуль не найден — 'leaflet'

**Ошибка:**
```
Module not found: Can't resolve 'leaflet'
```

**Местоположение:**
- `frontend/src/modules/routes/lib/providers/leaflet-map-provider.ts`

**Анализ:**
- В `package.json` отсутствует зависимость `leaflet`
- `LeafletMapProvider` импортирует `leaflet`, но пакет не установлен

**Рекомендация:**
1. Установить зависимости:
   ```bash
   npm install leaflet @types/leaflet
   ```
2. Или сделать Leaflet опциональным (динамический импорт только при использовании)

---

## 3. Ручные сценарии тестирования

### 3.1. Подготовка к тестированию

**Требования:**
1. ✅ Backend запущен на `http://localhost:5000`
2. ✅ Frontend запущен на `http://localhost:3000`
3. ❌ API ключ Yandex Maps настроен
4. ❌ Leaflet установлен (если используется fallback)

### 3.2. Список действий для ручного тестирования

#### Сценарий 1: Открытие страницы RouteDetailsView

**Действия:**
1. Открыть браузер
2. Перейти на `http://localhost:3000/routes?from=Якутск&to=Нерюнгри&date=2024-12-25`
3. Выбрать маршрут из списка
4. Перейти на страницу деталей маршрута

**Ожидаемый результат:**
- Страница загружается без ошибок
- Все секции маршрута отображаются
- Секция карты присутствует на странице

**Проверка:**
- [ ] Страница загружается
- [ ] Нет ошибок в консоли браузера
- [ ] Компонент `RouteMapSection` отображается

#### Сценарий 2: Открытие и инициализация карты

**Действия:**
1. Прокрутить до секции карты
2. Дождаться загрузки данных карты
3. Проверить инициализацию карты

**Ожидаемый результат:**
- Карта инициализируется
- Отображается контейнер карты
- Нет ошибок в консоли

**Проверка:**
- [ ] Контейнер карты виден
- [ ] Карта инициализирована (проверить через DevTools: `window.ymaps` или `window.L`)
- [ ] Нет ошибок типа "Yandex Maps API is not loaded"
- [ ] Нет ошибок типа "Failed to initialize map"

#### Сценарий 3: Рендер сегментов маршрута

**Действия:**
1. Дождаться загрузки данных маршрута
2. Проверить отображение полилиний на карте

**Ожидаемый результат:**
- Все сегменты маршрута отображаются полилиниями
- Цвета полилиний соответствуют типу транспорта:
  - Синий (#0066cc) — самолёт
  - Бирюзовый (#00a8b5) — автобус
  - Оранжевый (#ff9900) — поезд
  - Зелёный (#00cc66) — паром
  - Красный (#cc0000) — такси

**Проверка:**
- [ ] Полилинии отображаются
- [ ] Цвета соответствуют типам транспорта
- [ ] Для авиамаршрутов используется Great Circle (дуга)
- [ ] Для наземного транспорта используется прямая линия

#### Сценарий 4: Маркеры start/end/transfer

**Действия:**
1. Проверить наличие маркеров на карте
2. Кликнуть на каждый маркер

**Ожидаемый результат:**
- Маркер начала маршрута (📍 зелёный) в точке отправления
- Маркер конца маршрута (🏁 красный) в точке прибытия
- Маркеры пересадок (🔄 серый) в точках пересадок
- При клике открывается popup с информацией об остановке

**Проверка:**
- [ ] Маркер начала отображается
- [ ] Маркер конца отображается
- [ ] Маркеры пересадок отображаются (если есть)
- [ ] Popup открывается при клике
- [ ] Информация в popup корректна

#### Сценарий 5: Переключение альтернативных маршрутов

**Действия:**
1. Если есть альтернативные маршруты, проверить переключатель
2. Кликнуть на кнопку "Следующий маршрут" (→)
3. Кликнуть на кнопку "Предыдущий маршрут" (←)
4. Кликнуть на индикатор маршрута (1, 2, 3...)

**Ожидаемый результат:**
- Переключатель отображается, если есть альтернативы
- При переключении карта обновляется
- Полилинии и маркеры обновляются
- Позиция карты сохраняется (если `preserveMapPosition=true`)

**Проверка:**
- [ ] Переключатель отображается
- [ ] Кнопки навигации работают
- [ ] Индикаторы маршрутов кликабельны
- [ ] Карта обновляется при переключении
- [ ] Нет мерцания или задержек

#### Сценарий 6: Работа fallback (если Yandex недоступен)

**Действия:**
1. Отключить API ключ Yandex Maps (установить пустое значение)
2. Перезагрузить страницу
3. Проверить, что карта всё равно работает (через Leaflet)

**Ожидаемый результат:**
- Если Yandex недоступен, используется Leaflet
- Карта отображается с OpenStreetMap тайлами
- Функциональность не теряется

**Проверка:**
- [ ] Fallback на Leaflet работает
- [ ] Карта отображается
- [ ] Нет ошибок в консоли
- [ ] Все функции работают

**Примечание:** Требуется установить `leaflet` и настроить fallback логику

#### Сценарий 7: Работа на разных размерах экрана

**Действия:**
1. Открыть DevTools
2. Переключиться в режим Responsive Design
3. Проверить на размерах:
   - Mobile (375px × 667px)
   - Tablet (768px × 1024px)
   - Desktop (1920px × 1080px)

**Ожидаемый результат:**
- Карта адаптируется под размер экрана
- Легенда не перекрывает карту
- Контролы карты доступны
- Нет горизонтального скролла

**Проверка:**
- [ ] Mobile: карта отображается корректно
- [ ] Tablet: карта отображается корректно
- [ ] Desktop: карта отображается корректно
- [ ] Легенда не перекрывает контент
- [ ] Контролы доступны

#### Сценарий 8: Выявление визуальных и UX-багов

**Действия:**
1. Проверить все элементы интерфейса
2. Проверить анимации и переходы
3. Проверить доступность (accessibility)

**Проверка:**
- [ ] Нет визуальных глитчей
- [ ] Анимации плавные
- [ ] Цвета контрастны (WCAG AA)
- [ ] Элементы доступны с клавиатуры
- [ ] Screen reader может прочитать информацию
- [ ] Нет проблем с производительностью (FPS > 30)

---

## 4. Найденные баги

### Критические (блокируют запуск)

1. **Баг #1: Модуль '../route-map/ui' не найден**
   - **Приоритет:** Критический
   - **Статус:** Не исправлен
   - **Описание:** Next.js не может разрешить импорт `'../route-map/ui'` в `route-details-view.tsx`
   - **Влияние:** Приложение не компилируется, E2E тесты не запускаются
   - **Решение:** Использовать абсолютный импорт или barrel export

2. **Баг #2: Пакет 'leaflet' не установлен**
   - **Приоритет:** Критический
   - **Статус:** Не исправлен
   - **Описание:** `LeafletMapProvider` требует `leaflet`, но пакет отсутствует в `package.json`
   - **Влияние:** Приложение не компилируется, fallback на Leaflet не работает
   - **Решение:** Установить `leaflet` и `@types/leaflet`

### Важные (требуют внимания)

3. **Баг #3: API ключ Yandex Maps не настроен**
   - **Приоритет:** Высокий
   - **Статус:** Не исправлен
   - **Описание:** Переменная `NEXT_PUBLIC_YANDEX_MAPS_API_KEY` не задана
   - **Влияние:** Yandex Maps не загрузится, карта не будет работать
   - **Решение:** Создать `.env.local` и добавить API ключ

### Средние (можно отложить)

4. **Баг #4: Отсутствует обработка ошибок загрузки Yandex Maps**
   - **Приоритет:** Средний
   - **Статус:** Не исправлен
   - **Описание:** Нет автоматического fallback на Leaflet при ошибке загрузки Yandex Maps
   - **Влияние:** Пользователь увидит ошибку вместо карты
   - **Решение:** Добавить обработку ошибок и fallback логику

---

## 5. Рекомендации

### 5.1. Немедленные действия (перед запуском)

1. **Исправить импорт в `route-details-view.tsx`:**
   ```typescript
   // Было:
   import { RouteMapSection } from '../route-map/ui';
   
   // Стало:
   import { RouteMapSection } from '@/modules/routes/features/route-map/ui';
   ```

2. **Установить Leaflet:**
   ```bash
   npm install leaflet @types/leaflet
   ```

3. **Создать `.env.local` и добавить API ключ:**
   ```env
   NEXT_PUBLIC_YANDEX_MAPS_API_KEY=ваш_ключ_здесь
   ```

4. **Очистить кэш Next.js:**
   ```bash
   rm -rf .next
   npm run build
   ```

### 5.2. Улучшения (после исправления критических багов)

1. **Добавить обработку ошибок загрузки Yandex Maps:**
   - Проверять наличие `window.ymaps` после загрузки скрипта
   - Автоматически переключаться на Leaflet при ошибке
   - Показывать сообщение пользователю

2. **Добавить loading state для карты:**
   - Показывать скелетон во время загрузки
   - Показывать прогресс-бар при инициализации

3. **Оптимизировать производительность:**
   - Lazy load карты (только при скролле до секции)
   - Мемоизировать вычисления границ карты
   - Использовать `React.memo` для компонентов карты

4. **Улучшить accessibility:**
   - Добавить ARIA-атрибуты
   - Обеспечить навигацию с клавиатуры
   - Добавить альтернативный текст для маркеров

---

## 6. Задачи, которые ещё нужно выполнить

### Критические (блокируют запуск)

- [ ] Исправить импорт `'../route-map/ui'` в `route-details-view.tsx`
- [ ] Установить `leaflet` и `@types/leaflet`
- [ ] Создать `.env.local` с API ключом Yandex Maps
- [ ] Очистить кэш Next.js и пересобрать проект
- [ ] Запустить E2E тесты и убедиться, что они проходят

### Важные (перед продакшеном)

- [ ] Добавить обработку ошибок загрузки Yandex Maps
- [ ] Реализовать fallback на Leaflet
- [ ] Провести ручное тестирование всех сценариев
- [ ] Исправить найденные визуальные и UX-баги
- [ ] Проверить работу на разных браузерах (Chrome, Firefox, Safari)
- [ ] Проверить работу на мобильных устройствах

### Опциональные (улучшения)

- [ ] Добавить lazy loading для карты
- [ ] Оптимизировать производительность
- [ ] Улучшить accessibility
- [ ] Добавить unit тесты для компонентов карты (если ещё не все покрыты)
- [ ] Добавить интеграционные тесты для fallback логики

---

## 7. Готовность карты к продакшену

### Текущий статус: ❌ НЕ ГОТОВА

**Причины:**
1. Критические баги блокируют компиляцию
2. E2E тесты не запускаются
3. Ручное тестирование не проведено
4. API ключ не настроен

### Критерии готовности к продакшену

**Минимальные требования:**
- [x] Backend endpoint `/api/v1/routes/map` работает
- [x] Frontend компоненты созданы
- [x] Unit тесты написаны и проходят
- [ ] Приложение компилируется без ошибок
- [ ] E2E тесты написаны и проходят
- [ ] API ключ Yandex Maps настроен
- [ ] Ручное тестирование проведено
- [ ] Критические баги исправлены

**Рекомендуемые требования:**
- [ ] Fallback на Leaflet реализован
- [ ] Обработка ошибок добавлена
- [ ] Производительность оптимизирована
- [ ] Accessibility улучшена
- [ ] Тестирование на разных устройствах проведено

### Оценка времени до готовности

**Минимум (критические исправления):**
- Исправление импорта: 5 минут
- Установка Leaflet: 2 минуты
- Настройка API ключа: 5 минут
- Пересборка и проверка: 10 минут
- **Итого: ~20-30 минут**

**Рекомендуемый (с улучшениями):**
- Критические исправления: 30 минут
- Добавление fallback: 1 час
- Ручное тестирование: 1-2 часа
- Исправление найденных багов: 1-2 часа
- **Итого: ~3-5 часов**

---

## 8. Заключение

Интеграция карты маршрутов **почти завершена**, но есть **критические проблемы**, которые блокируют запуск:

1. Проблема с импортом модуля
2. Отсутствие пакета Leaflet
3. Не настроен API ключ Yandex Maps

После исправления этих проблем и проведения ручного тестирования карта будет готова к использованию.

**Следующий шаг:** Исправить критические баги и запустить E2E тесты.

---

**Отчёт подготовлен:** 2025-01-27  
**Версия:** 1.0







