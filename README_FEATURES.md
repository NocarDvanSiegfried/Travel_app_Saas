# 🚀 Новые функции Travel App SaaS

Документация по новым функциям: загрузка изображений туров, обновлённое страхование и адаптивный информационный блок.

---

## 📋 Содержание

1. [Загрузка изображений туров](#-загрузка-изображений-туров)
2. [Страхование путешествий](#-страхование-путешествий)
3. [Информационный блок](#-информационный-блок)
4. [Установка и настройка](#-установка-и-настройка)
5. [Использование](#-использование)
6. [Тестирование](#-тестирование)

---

## 🖼️ Загрузка изображений туров

### Обзор
Система позволяет загружать, управлять и отображать изображения для туров с автоматической обработкой и хранением.

### Возможности
- **Drag & Drop загрузка** с предпросмотром
- **Автоматическая обработка**: Создание thumbnail и оптимизированных версий
- **Двойное хранилище**: MinIO (S3) + локальное хранилище с автоматическим фоллбэком
- **Метаданные**: Альтернативный текст, сортировка, главное изображение
- **Безопасность**: Валидация типов файлов, ограничение размера, защита от path traversal

### API Endpoints
```http
POST   /api/v1/tours/:tourId/images          - Загрузка изображений
GET    /api/v1/tours/:tourId/images          - Получение изображений тура
PUT    /api/v1/tours/:tourId/images/:imageId/main - Установка главного изображения
DELETE /api/v1/tours/:tourId/images/:imageId      - Удаление изображения
```

### Использование в Frontend
```typescript
import { ImageUpload } from '@/components/ui/ImageUpload';
import { useTourImages } from '@/hooks/useTourImages';

const TourImageManager = () => {
  const { images, uploadImages, deleteImage } = useTourImages(tourId);

  return (
    <ImageUpload
      tourId={tourId}
      onImagesChange={uploadImages}
      maxImages={20}
      maxFileSize={5 * 1024 * 1024} // 5MB
    />
  );
};
```

### Администрирование
- **URL**: `/tours/[id]/images`
- **Функциональность**:
  - Загрузка и удаление изображений
  - Установка главного изображения
  - Изменение порядка отображения
  - Просмотр статистики хранилища

---

## 🛡️ Страхование путешествий

### Новые страницы

#### 1. Условия страхования (`/insurance/conditions`)
- Детальные правила покрытия для этапной и сквозной страховки
- Исключения и ограничения
- Возрастные ограничения и требования
- Действия при страховых случаях

#### 2. Как оформить (`/insurance/how-to`)
- Пошаговые инструкции для онлайн и офисного оформления
- Необходимые документы
- Время оформления и способы оплаты
- Экстренные контакты

#### 3. Вопросы и ответы (`/insurance/faq`)
- 20+ частых вопросов с ответами
- Поиск и фильтрация по категориям
- Интерактивное раскрытие ответов

### Типы страхования

#### Этапная страховка (300 ₽)
- Покрытие: задержки, отмены, потеря багажа
- Одно путешествие/один билет
- Компенсация до 1000 ₽ за задержку

#### Сквозная страховка (от 1500 ₽)
- Покрытие: отмена поездки, прерывание, экстренная эвакуация
- Всё путешествие целиком
- Защита от крупных финансовых рисков

### Интеграция в навигацию
- **Header**: Акцентная кнопка "Страхование" с иконкой щита
- **Страница страхования**: Навигация по подразделам
- **SEO оптимизация**: Полная семантическая разметка

---

## 📱 Адаптивный информационный блок

### Обзор
Динамический контент-блок под формой поиска маршрутов с персонализированными рекомендациями.

### Типы контента
- **Рекламации**: Путешествия, отели, услуги
- **Промоции**: Скидки и акции
- **Погода**: Текущая погода в регионе
- **Новости**: Обновления маршрутов и события
- **Информационные**: Советы и инструкции

### Технические особенности
- **Кеширование**: Redis с TTL 5 минут
- **Персонализация**: По устройству, региону, времени года
- **Responsive**: Разные отображения для mobile/desktop
- **Health monitoring**: Автоматическое восстановление при ошибках

### API Endpoint
```http
GET /api/v1/content/route-sidebar?device=desktop&region=yakutsk&limit=6
```

### Фронтенд компонент
```typescript
import { RouteInfoBlock } from '@/components/ui/RouteInfoBlock';

<RouteInfoBlock
  routeId="tour-123"
  maxItems={6}
  collapsible={true}
  device="desktop"
/>
```

---

## ⚙️ Установка и настройка

### Требования
- Docker и Docker Compose
- Node.js 18+
- PostgreSQL 15+
- Redis 7+
- MinIO (S3-совместимое хранилище)

### 1. Клонирование и запуск
```bash
git clone <repository-url>
cd Travel_app_Saas
cp .env.example .env
docker compose up --build
```

### 2. Настройка MinIO
```bash
# Создание бакета
docker compose exec minio bash -c "
mc alias set myminio http://localhost:9000 minioadmin minioadmin
mc mb myminio/travel-app
mc policy set public myminio/travel-app
"

# Проверка
curl http://localhost:9000/minio/health/live
```

### 3. Применение миграций
```bash
# Автоматически при старте
docker compose exec backend npm run build

# или вручную
docker compose exec backend psql -U travel_user -d travel_app \
  -f /app/infrastructure/database/migrations/004_create_tour_images_table.sql
```

### 4. Переменные окружения
```env
# Хранилище изображений
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=travel-app
STORAGE_LOCAL_PATH=./uploads

# Ограничения загрузки
MAX_IMAGE_SIZE=5242880  # 5MB
MAX_IMAGES_PER_TOUR=20

# База данных
POSTGRES_DB=travel_app
POSTGRES_USER=travel_user
POSTGRES_PASSWORD=your_password

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
```

---

## 🎯 Использование

### Администрирование изображений

1. **Загрузка изображений**
   ```bash
   # Via UI
   curl -X POST http://localhost:3000/tours/tour-id/images \
     -F "files=@image1.jpg" \
     -F "files=@image2.png"
   ```

2. **Установка главного изображения**
   ```http
   PUT /api/v1/tours/tour-id/images/image-id/main
   ```

3. **Получение статистики**
   ```http
   GET /api/v1/tours/tour-id/images/stats
   ```

### Управление контентом

1. **Получение контента для боковой панели**
   ```http
   GET /api/v1/content/route-sidebar?device=desktop&region=yakutsk
   ```

2. **Очистка кеша**
   ```http
   POST /api/v1/content/clear-cache
   ```

### Мониторинг

1. **Health Checks**
   ```bash
   # Storage provider health
   curl http://localhost:5000/api/v1/storage/provider

   # Database health
   curl http://localhost:5000/health/database

   # Redis health
   curl http://localhost:5000/health/redis
   ```

2. **Логи**
   ```bash
   # Application logs
   docker compose logs -f backend

   # MinIO logs
   docker compose logs -f minio

   # Redis logs
   docker compose logs -f redis
   ```

---

## 🧪 Тестирование

### Backend Tests
```bash
cd backend

# Unit тесты
npm run test:unit

# Интеграционные тесты
npm run test:integration

# E2E тесты
npm run test:e2e

# Покрытие
npm run test:coverage
```

### Frontend Tests
```bash
cd frontend

# Unit тесты
npm run test

# E2E тесты (Playwright)
npm run test:e2e

# Покрытие
npm run test:coverage
```

### Ключевые тестовые сценарии

#### Изображения
- Загрузка различных форматов (JPEG, PNG, WebP)
- Обработка больших файлов (>5MB)
- Недопустимые форматы файлов
- Поведение при недоступности MinIO
- Автоматическое создание thumbnail

#### Контент
- Кеширование и инвалидация
- Фильтрация по устройству
- Региональная персонализация
- Обработка ошибок API

---

## 🐛 Траблшутинг

### Проблемы с загрузкой изображений

**MinIO недоступен**
```bash
# Проверка статуса
curl http://localhost:9000/minio/health/live

# Просмотр логов
docker compose logs minio

# Перезапуск
docker compose restart minio
```

**Права доступа к директории uploads**
```bash
# Проверка прав
ls -la ./uploads

# Исправление
sudo chown -R $(id -u):$(id -g) ./uploads
chmod 755 ./uploads
```

### Проблемы с контентом

**Redis недоступен**
```bash
# Проверка подключения
docker compose exec redis redis-cli ping

# Перезапуск
docker compose restart redis
```

**API ошибки**
```bash
# Проверка health endpoint
curl http://localhost:5000/health

# Просмотр логов приложения
docker compose logs backend
```

---

## 📊 Производительность

### Оптимизация изображений
- **Automatic resizing**: thumbnail (400x300), optimized (1200x900)
- **Compression**: JPEG quality 85% для optimized версии
- **Lazy loading**: При открытии галереи

### Кеширование
- **Redis**: Контент кешируется на 5 минут
- **Browser**: Client-side кеширование для статических ресурсов
- **CDN**: Через MinIO public URLs

### Мониторинг производительности
- **API Response Time**: <200ms для контент endpoints
- **Image Processing**: <1сек для thumbnail генерации
- **Upload Speed**: Зависит от размера файла и сети

---

## 🔒 Безопасность

### Валидация файлов
- **Типы MIME**: Только image/jpeg, image/png, image/webp, image/gif
- **Размер**: Максимум 5MB (настраивается)
- **Path Traversal**: Блокировка `../` в именах файлов

### Авторизация
- JWT токены для API доступа
- Проверка владения туром
- Rate limiting для загрузки

### Защита данных
- Анонимизация логов
- Валидация входных данных
- Защита от XSS и SQL-инъекций

---

## 📈 Масштабирование

### Horizontal Scaling
- **Frontend**: Multiple instances за балансировщиком
- **Backend**: Auto-scaling группы инстансов
- **MinIO**: Distributed storage mode
- **Redis**: Redis Cluster

### Vertical Scaling
- **CPU**: Увеличение для image processing
- **Memory**: Кеширование в памяти
- **Storage**: SSD для быстрого доступа к файлам

---

## 🔮 Roadmap

### В планах
- [ ] Видеогалереи для туров
- [ ] AI-генерация описаний изображений
- [ ] CDN интеграция
- [ ] Real-time уведомления
- [ ] Mobile приложение

### Расширение функциональности
- [ ] Watermarking изображений
- [ ] Биллинг за хранилище
- [ ] API для внешних приложений
- [ ] Аналитика использования

---

## 📞 Поддержка

### Техническая поддержка
- **Email**: support@travelapp.ru
- **Telegram**: @travelapp-tech
- **GitHub**: [Issues](https://github.com/travelapp-saas/issues)

### Документация
- [API Documentation](http://localhost:5000/api-docs)
- [Developer Guide](./docs/DEVELOPER.md)
- [Architecture](./architecture/)

### Сообщество
- **Форум**: forum.travelapp.ru
- **Discord**: discord.gg/travelapp
- **Blog**: blog.travelapp.ru

---

## 🏷️ Версионирование

- **Backend**: v2.1.0
- **Frontend**: v2.1.0
- **Database**: Migration 004
- **API**: v1

**Semantic Versioning**: Следуем [semver.org](https://semver.org/)

---

## ✅ Checklist перед продакшеном

- [ ] Все тесты пройдены
- [ ] Линтинг успешен
- [ ] Билд успешен
- [ ] Миграции применены
- [ ] MinIO настроен
- [ ] Redis доступен
- [ ] Health checks проходят
- [ ] Документация обновлена

---

## 🎉 Заключение

Новые функции значительно улучшают пользовательский опыт:
- **Визуальный контент**: Изображения делают туры более привлекательными
- **Информационная поддержка**: Адаптивный блок помогает пользователям
- **Улучшенное страхование**: Полная документация и удобное оформление

Спасибо за использование Travel App SaaS! 🚀